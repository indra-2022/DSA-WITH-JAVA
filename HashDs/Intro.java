package HashDs;

public class Intro {
   public static void main(String[] args) {
    String nm="Indra";
    System.out.println(nm.hashCode());// It will convert it to a hashcode,usin some 
    //formula, it will be the same everytime
    Integer a=10;
    System.out.println(a.hashCode());// Usually INTEGER hash code is the sm as the data
    //We use INTEGER not int 
   }
}
